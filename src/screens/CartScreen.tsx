import React from "react";
import { Text, View } from "react-native";

export default function CartScreen({css}) {
    return (
        <View style={[css,]}>
            <Text>Cart Page</Text>
        </View>
    )
}