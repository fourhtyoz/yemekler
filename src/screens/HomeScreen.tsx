import React from "react";
import { Text, View } from "react-native";

export default function HomeScreen({css}) {
    return (
        <View style={[css,]}>
            <Text>Home Page</Text>
        </View>
    )
}