import React from "react";
import { Text, View } from "react-native";

export default function HelpScreen({css}) {
    return (
        <View style={[css,]}>
            <Text>Help Page</Text>
        </View>
    )
}