import React from "react";
import { Text, View, } from "react-native";

export default function SearchScreen({css}) {
    return (
        <View style={[css,]}>
            <Text>Search Page</Text>
        </View>
    )
}
